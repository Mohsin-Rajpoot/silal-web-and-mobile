import { View, Text } from "react-native";
import React from "react";
import CommonStyle from "../../../styles";
const index = () => {
  return <View style={CommonStyle.commonIcon} />;
};

export default index;
